# 🟢 VGA Matrix Text on FPGA (Basys 2 / Spartan-3E)

> **FOLLOW THE WHITE RABBIT** 🐇
>
> Минимальный, но показательный проект по генерации VGA-сигнала на FPGA.
> Никаких CPU, ОС или видеокарт — только логика, тайминги и тактовый сигнал.

---

## 📌 О проекте

Этот репозиторий содержит учебный проект для платы **Digilent Basys 2**
с FPGA **Spartan‑3E (XC3S100E)**. Проект демонстрирует:

* аппаратную генерацию VGA‑сигнала (640×480 @ 60 Гц)
* вывод текста **"FOLLOW THE WHITE RABBIT"** в стиле *Матрицы*
* работу строго на одном тактовом сигнале (50 МГц)
* корректный подход без DCM и без «самодельных» клоков

Проект специально сделан **максимально прозрачным** — его удобно разбирать
на занятиях по цифровой схемотехнике и FPGA.

---

## 🧠 Ключевая идея

FPGA сама формирует видеосигнал:

* горизонтальную и вертикальную развёртку
* синхроимпульсы `HS` и `VS`
* состояние каждого пикселя
* символы берутся из ROM‑таблицы шрифта

Каждый пиксель на экране — результат логического выражения.

> Это не «картинка», это **реальность, собранная из тактов**.

---

## 🖥 Параметры VGA

| Параметр    | Значение                     |
| ----------- | ---------------------------- |
| Разрешение  | 640 × 480                    |
| Частота     | ~60 Гц                       |
| Pixel clock | ~25 МГц (через pixel‑enable) |
| Цвет        | монохромный зелёный          |

---

## 📂 Состав проекта

* `vga_matrix_text.vhd` — **top‑level модуль** (VGA + текст)
* `vga_matrix_text_tb.vhd` — тестбенч для ISim (Behavioral Simulation)
* `Basys2_100_250General.ucf` — UCF с **только используемыми пинами**
* `vga_matrix_text.xise` — файл проекта Xilinx ISE

> ⚠️ Генератор проектов автоматически переименовывает файлы под имя проекта.
> Например: `P05_VGA_Text.vhd`, `P05_VGA_Text_tb.vhd`.

---

## 🚀 Быстрый старт (Xilinx ISE)

1. Откройте файл `*.xise` в **Xilinx ISE Project Navigator**.
2. Проверьте настройки прошивки:
   `Generate Programming File → Process Properties → Startup Options → FPGA Start‑Up Clock = JTAG Clock`.
3. Запустите сборку:

   * `Synthesize`
   * `Implement Design`
   * `Generate Programming File`
4. Прошейте `.bit` файл через **Digilent Adept**.
5. Подключите VGA‑монитор — текст появится автоматически.

---

## 🧪 Симуляция (ISim)

Тестбенч расположен рядом с top‑модулем: `*_tb.vhd`.

Порядок:

1. Переключитесь в **Simulation View**.
2. Убедитесь, что тестбенч выбран как **Top for Behavioral Simulation**.
3. Запустите `Simulate Behavioral Model`.

💡 Подсказки:

* по умолчанию время симуляции может быть слишком маленьким (например 1000 ns)
* `U` / `X` в волновых диаграммах обычно означают неинициализированные сигналы

---

## ✍️ Стиль и договорённости VHDL

* один глобальный тактовый сигнал (`MCLK`)
* **pixel‑enable**, а не отдельный pixel‑clock
* один процесс `rising_edge(MCLK)` для регистров
* комбинационная логика — через `process(all)` или concurrent assignments
* без `mod` не‑степеней двойки (дружелюбно к XST)

Такой стиль даёт:

* стабильный VGA без «ряби»
* предсказуемый тайминг
* чистую синтезируемость

---

## 🎓 Назначение проекта

Проект подходит для:

* студентов, изучающих **FPGA и VGA**
* лабораторных работ по цифровой логике
* демонстрации «железа без процессора»
* YouTube‑серий про low‑level FPGA

---

## 🟢 Атмосфера

> FOLLOW THE WHITE RABBIT
>
> Возможно, Матрица — это не симуляция.
> Возможно, это просто очень хорошо синхронизированная схема.

---

**Плата:** Digilent Basys 2  
**FPGA:** Spartan‑3E XC3S100E‑CP132  
**Инструменты:** Xilinx ISE + ISim  
**Язык:** VHDL
